{
  "name": "scholarstudio-spa",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "lint": "echo 'No linting needed for this SPA'"
  },
  "dependencies": {
    "@google/genai": "^1.46.0",
    "marked": "^17.0.5",
    "vite": "^6.2.0"
  }
}
